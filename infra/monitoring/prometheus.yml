global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "ledger-service"
    static_configs:
      - targets: ["ledger-api:8000"]
    metrics_path: /metrics

  - job_name: "consolidation-service"
    static_configs:
      - targets: ["consolidation-api:8000"]
    metrics_path: /metrics
